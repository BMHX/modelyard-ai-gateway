# Deprecated.
# Runtime commands no longer read `.env` or `.env.local`.
#
# Use one of these instead:
# - `cp .env.development.example .env.development`
# - `cp .env.production.example .env.production`
